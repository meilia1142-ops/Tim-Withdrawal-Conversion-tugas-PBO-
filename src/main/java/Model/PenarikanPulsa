package model;

import java.math.BigDecimal;

public class PenarikanPulsa extends Penarikan {

    public PenarikanPulsa(
            int idNasabah,
            BigDecimal nominal) {

        super(idNasabah, nominal);
    }

    @Override
    public void eksekusiPenarikan() {

        System.out.println(
                "Konversi Pulsa: " + nominal
        );
    }
}
